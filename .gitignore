# Godot 4+ specific ignores
.godot/
/android/
/build/
/export/
*.translation

# Godot export — export_presets.cfg IS committed (CI needs it). Only ignore
# the credentials siblings Godot generates when keystores / signing keys are
# configured locally.
export_presets.cfg.bak
*.keystore
*.jks

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.bak

# Claude local settings
.claude/settings.local.json
