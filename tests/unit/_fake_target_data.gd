extends RefCounted

# Minimal data stub for DamageCalculator tests. Mirrors the EnemyData shape
# DamageCalculator reads: `armor` and `magic_resist`, both clamped 0..1.

var armor: float = 0.0
var magic_resist: float = 0.0
